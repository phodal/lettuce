/* lettuce main */

// Base function.
var lettuce = function() {
  // Add functionality here.
  return true;
};


// Version.
lettuce.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.lettuce = lettuce;
